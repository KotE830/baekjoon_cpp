#include <cmath>
#include <iostream>

using namespace std;

int main() {
    double d = 4.5;
    int i = 9;

    ceil(d);  // 4
    floor(d); // 5
    round(d); // 5

    pow(i, 2); // 81
    sqrt(i);   // 3

    return 0;
}